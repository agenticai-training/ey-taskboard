# Search Requirements Checklist: Search Tasks on the Board

**Purpose**: PR reviewer checklist validating requirements quality for server-side search (match rules, normalization, contract parity) and board UX (status-filter composition, no-match empty state, search control).
**Created**: 2026-09-24
**Feature**: [spec.md](../spec.md)
**Audience**: Reviewer (PR) | **Depth**: Standard
**Focus**: (1) Server-side search by title/description/assignee with contains match and query normalization; (2) Composition with status filter plus no-match empty state and board search UX

**Note**: This custom checklist is generated by the `/speckit-checklist` command based on feature context and requirements.
**Review Ownership**: This checklist is a reviewer-owned requirements-quality review artifact. Mark an item `[x]` only when the reviewer determines the requirements-quality criterion is satisfied.
**Marker Semantics**: `[x]` means the criterion has been reviewed and satisfied for requirements quality. It does not mean implementation work is complete.

## Requirement Completeness — Server-Side Search

- [ ] CHK001 Are searchable fields explicitly limited to title, description, and assignee with exclusion of comments, id, status text, and timestamps documented? [Completeness, Spec §FR-004, Spec §FR-012]
- [ ] CHK002 Are server-side execution requirements stated (search as a task-list query, not client-only filtering of loaded tasks)? [Completeness, Spec §FR-014, Spec §Clarifications]
- [ ] CHK003 Are cross-stack parity requirements defined for query parameter name, normalization rules, match semantics, and JSON response shape? [Completeness, Spec §FR-014, Contract §GET /api/tasks]
- [ ] CHK004 Are all query-normalization rules documented: trim, inactive when empty/all-whitespace or trimmed length < 3, and truncate to 200 when trimmed length > 200? [Completeness, Spec §FR-006, Contract §Normalization]
- [ ] CHK005 Are requirements defined for matching against the full stored description value even when the card truncates or hides text? [Completeness, Spec §FR-004, Spec §Clarifications]
- [ ] CHK006 Are OR-match semantics specified (a match on any one of title, description, or assignee is sufficient)? [Completeness, Spec §FR-004, Spec §US1]
- [ ] CHK007 Are explicit out-of-scope search capabilities listed (fuzzy, synonyms, stemming, ranked relevance, saved searches, pagination, separate results page)? [Completeness, Spec §FR-013, Spec §Out of Scope]

## Requirement Clarity — Match Rules & Normalization

- [ ] CHK008 Is "keyword / contains" match defined unambiguously as case-insensitive substring search with a concrete example? [Clarity, Spec §FR-003, Spec §Edge Cases]
- [ ] CHK009 Is the minimum active query length (≥ 3 characters after trim) stated consistently across spec, contract, and data model? [Clarity, Spec §FR-006, Contract §Normalization, Data Model §Search query]
- [ ] CHK010 Is the maximum query length (200 characters after trim, truncate longer input) defined with identical client and backend treatment? [Clarity, Spec §FR-006, Contract §Normalization]
- [ ] CHK011 Is inactive-query behaviour defined as non-error status-filter-only results rather than a validation failure? [Clarity, Spec §FR-006, Contract §Error contract]
- [ ] CHK012 Is behaviour for null or empty description/assignee fields specified for contains matching? [Clarity, Data Model §Task, Gap if absent from spec body]
- [ ] CHK013 Is the HTTP parameter name for search (`q`) documented in the contract with passthrough expectations for all three backends? [Clarity, Contract §GET /api/tasks, Plan §Summary]

## Requirement Consistency — Contract & Layering

- [ ] CHK014 Are layering requirements consistent between spec and plan (normalization in service, SQL in repository, HTTP mapping in controller/router, frontend HTTP only in services)? [Consistency, Spec §FR-016, Plan §Constitution Check]
- [ ] CHK015 Do error-contract requirements remain unchanged for unknown status (`422`) and missing id (`404`) with no new codes introduced for search? [Consistency, Spec §FR-014, Spec §FR-015, Contract §Error contract]
- [ ] CHK016 Are normalization rules aligned between the API contract, data model, and frontend wire-usage section without conflicting thresholds? [Consistency, Contract §Frontend wire usage, Data Model §Search query]
- [ ] CHK017 Do success-criteria statements for case-insensitive contains match align with functional requirements and user-story acceptance scenarios? [Consistency, Spec §SC-002, Spec §US1]

## Acceptance Criteria Quality — Search Behaviour

- [ ] CHK018 Can "case-insensitive contains match on title, description, or assignee" be objectively verified from written acceptance scenarios? [Measurability, Spec §US1, Spec §SC-001]
- [ ] CHK019 Are acceptance criteria defined for inactive queries (empty, whitespace-only, under 3 characters) restoring status-filter-only results? [Measurability, Spec §US3, Spec §SC-004]
- [ ] CHK020 Is the no-match API outcome specified as `200` with an empty array rather than an error response? [Measurability, Contract §Success, Spec §US4]
- [ ] CHK021 Are requirements for query lifetime across in-session board refresh versus full page reload stated as distinct, testable outcomes? [Measurability, Spec §FR-008, Spec §FR-017, Spec §SC-006, Spec §SC-008]

## Scenario Coverage — Status Filter Composition

- [ ] CHK022 Are requirements defined for applying status filter and active search together as an intersection (both constraints must hold)? [Coverage, Spec §FR-005, Spec §US2]
- [ ] CHK023 Do requirements specify that matching tasks remain in the correct status column when both filters are active? [Coverage, Spec §FR-005, Spec §US2]
- [ ] CHK024 Are column-count requirements defined to reflect the visible task set under combined status and search constraints? [Coverage, Spec §FR-005, Spec §SC-003]
- [ ] CHK025 Are requirements documented for clearing or deactivating search so the board returns to status-filter-only behaviour without regressing create, move, delete, comments, or status filter? [Coverage, Spec §FR-010, Spec §US3, Spec §SC-004]

## Scenario Coverage — Board Search UX & Empty States

- [ ] CHK026 Are search-control placement and composition requirements defined relative to existing board controls (status filter and refresh)? [Coverage, Spec §FR-001, Spec §US1]
- [ ] CHK027 Are search-control affordance requirements specified: visible label, clear placeholder, and one-action clear control? [Coverage, Spec §FR-001]
- [ ] CHK028 Is the global no-match empty-state copy exactly specified as "No tasks match your search" above the columns? [Clarity, Spec §FR-007, Spec §US4]
- [ ] CHK029 Are requirements defined for suppressing per-column "No tasks yet" while global no-match search is active? [Coverage, Spec §FR-007, Spec §US4, Spec §SC-005]
- [ ] CHK030 Are requirements defined for when per-column "No tasks yet" should still appear (search off, or not in global no-match state)? [Coverage, Spec §FR-007, Spec §US4]
- [ ] CHK031 Are same-board presentation requirements stated (no separate results page) with a qualitative immediacy expectation for typical team boards? [Coverage, Spec §FR-009, Spec §SC-007, Spec §Assumptions]

## Edge Case Coverage

- [ ] CHK032 Are edge cases for leading/trailing whitespace and all-whitespace input covered consistently in spec edge cases and functional requirements? [Edge Case, Spec §Edge Cases, Spec §FR-006]
- [ ] CHK033 Are edge cases for queries exceeding 200 characters after trim covered with truncate-not-reject behaviour on client and backends? [Edge Case, Spec §Edge Cases, Spec §FR-006]
- [ ] CHK034 Are partial-match scenarios across columns addressed (some columns empty under active search while others have matches vs global no-match)? [Edge Case, Spec §FR-007, Spec §US4]

## Dependencies, Assumptions & Traceability

- [ ] CHK035 Is the assumption that search actors are existing board users without new authentication or permissions documented and reflected in out-of-scope items? [Assumption, Spec §Assumptions, Spec §Out of Scope]
- [ ] CHK036 Does the spec reference or defer HTTP parameter naming to planning/contract artifacts without leaving match rules undefined? [Traceability, Spec §Assumptions, Contract §GET /api/tasks]
- [ ] CHK037 Are same-layer test requirements for new search behaviour documented as part of definition of done? [Completeness, Spec §FR-011, Spec §Assumptions]

## Notes

- Mark items `[x]` only after review confirms the requirement-quality criterion is satisfied
- Leave items unchecked when they still require clarification, correction, or reviewer evaluation
- `/speckit-implement` reads checklist checkbox state as a gate and must not modify markers
- `checklists/requirements.md` has a separate built-in lifecycle maintained by `/speckit-specify` and `/speckit-clarify`
- CHK012: null/empty field matching is explicit in `data-model.md`; confirm spec body or cross-artifact trace is sufficient for PR reviewers
- Add comments or findings inline
- Link to relevant resources or documentation
- Items are numbered sequentially for easy reference
